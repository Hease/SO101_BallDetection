# SO-101 납땜 시뮬레이션 실습 — 프로젝트 브리프

> 이 문서는 Claude Code 세션 시작 시 컨텍스트로 읽히기 위한 핸드오프 문서입니다.
> 코드를 작성하기 전에 **먼저 "0. 코드베이스 파악" 섹션의 조사를 수행하고, 발견한 내용을 사용자에게 보고한 뒤** 구현에 들어가십시오.

---

## 1. 프로젝트 개요

### 목표
SO-101 로봇암이 **인두기를 파지하여, 지정된 지점에 인두기 팁을 정확히 접촉시키는** 동작을 수행한다.
납땜 작업을 모사하는 것이며, **실제 가열·납 사용은 하지 않는다.**

### 타겟 정의
- 일자 드라이버 끝부분에 **색 마킹**을 해 둔다 → 이 마킹점이 "납땜할 지점" 역할
- 로봇암이 인두기 팁을 이 마킹점에 갖다 대면 성공

### 시퀀스
```
[1] 테이블 위 인두기 인식
[2] 인두기 파지 (grasp)
[3] 파지 상태 검증 / 팁 위치 파악
[4] 타겟(드라이버 마킹점) 상공으로 이동
[5] 정밀 접근 — 비전 피드백으로 오차 보정
[6] 접촉 → 유지 → 후퇴
```
**[2] 파지 액션부터 구현을 시작한다.**

---

## 2. 하드웨어 인벤토리

| 항목 | 내용 | 비고 |
|---|---|---|
| 로봇암 | SO-101 (Hiwonder / LeRobot 계열) | 5-DOF + 그리퍼, Feetech STS3215 서보 추정 |
| 손목 카메라 | 그리퍼 측면 장착 (eye-in-hand) | **핵심 센서. 아래 3장 참조** |
| 탑다운 카메라 | 클램프 스탠드 고정, **뎁스 카메라** | 사양 낮음. 코스 단계용 |
| 도구 | HAKKO PRESTO 인두기 (20W/50W) | 미가열 상태로 사용 |
| 타겟 | 일자 드라이버 | 팁에 색 마킹 |
| 고정 | 로봇암은 클램프로 책상에 고정됨 | |

### 예상 관절 구성 (코드에서 실제 이름 확인 필요)
`shoulder_pan`, `shoulder_lift`, `elbow_flex`, `wrist_flex`, `wrist_roll`, `gripper`

> ⚠️ **DOF 주의**: SO-101은 팔이 5-DOF다. 인두기를 잡으면 제어해야 할 것은
> "팁 위치(3) + 인두기 축 방향(2) = 5"로 **딱 맞아떨어진다. 여유가 없다.**
> 따라서 접근 각도를 임의로 바꾸면 IK가 풀리지 않을 수 있다.
> **접근 자세를 하나로 고정**하고 그 근방에서만 움직이는 설계로 갈 것.

---

## 3. ⛔ 제약 조건 (설계 시 반드시 준수)

이 프로젝트는 **금일 17시 마감의 단기 실습**이다. 다음은 협상 불가한 제약이다.

1. **3D 프린팅 불가** — 커스텀 그리퍼 조, 인두기 거치 지그 등 제작 불가
2. **하드웨어 개조 불가** — 있는 그대로 사용
3. **시간 극히 제한적** — 데이터 수집이 필요한 학습 기반 정책(ACT, Diffusion Policy 등) **사용하지 않는다**
4. **복잡한 아키텍처 금지** — 동작하는 최소 구현을 먼저 확보하고 단계적으로 올린다
5. 새 라이브러리 도입은 최소화 — 기존 코드베이스가 쓰는 스택을 그대로 활용

사용 가능한 재료: 마스킹 테이프, 컬러 테이프(청/적), 유성 마커 정도.

---

## 4. 핵심 문제와 해결 전략

### 4.1 문제 정의

**(A) 파지 편차** — 인두기를 잡을 때마다 그리퍼 기준 인두기의 자세가 미세하게 달라진다.
인두기는 파지점에서 팁까지 10~15cm이므로 **그립에서 2° 회전 오차 → 팁에서 약 5mm 오차**.
위치 오차보다 **회전 오차가 지배적**이다. 특히 원통형 배럴이라 축 방향 롤이 미끄러진다.

**(B) 접촉 시 밀림** — 접촉 반력으로 인두기가 그립 안에서 밀린다.

**(C) 그리퍼 형상 부적합** — SO-101 그리퍼는 원통 공구 파지에 최적화되어 있지 않다.

### 4.2 전략: 손목 카메라 기반 Teach-and-Repeat 비주얼 서보잉

지그를 만들 수 없으므로 **기구적 해결을 포기하고 전량 비전 폐루프로 흡수한다.**
손목 카메라가 있으므로 이게 가능하다.

#### 핵심 아이디어

인두기는 그리퍼에 **강체로** 물려 있다. 따라서 손목 카메라 영상에서
**인두기 팁은 항상 거의 같은 픽셀 위치에 나타난다** — 팔이 어떻게 움직이든 상관없이.

→ 파지 편차는 "그 고정 픽셀 위치가 어디로 이동했는가"로만 나타난다.
→ **월드 좌표계 TCP 계산이 전혀 필요 없다.**

#### Teach 단계 (파지 1회당 약 30초)

1. 로봇이 인두기를 파지한다
2. 사람이 텔레옵(리더암) 또는 조그 제어로 **인두기 팁을 타겟 마킹점에 실제로 접촉**시킨다
3. 이 순간을 기록한다:
   - 손목 카메라 영상에서 **타겟 마커가 찍힌 픽셀 좌표** → `GOAL_PIXEL`
   - 타겟 마커의 **겉보기 크기(면적/폭)** → `GOAL_SCALE` (거리 대용)
   - 전체 관절 각도 → `TEACH_POSE`

#### Repeat 단계 (자동 실행)

타겟이 어디에 있든, 손목 카메라에서 **타겟 마커가 `GOAL_PIXEL`에 오고 `GOAL_SCALE` 크기가 될 때까지** 팔을 움직인다. 도달하면 팁은 물리적으로 타겟에 닿아 있다.

```python
while True:
    u, v, area = detect_target_marker(wrist_cam)
    err_uv    = GOAL_PIXEL - (u, v)      # 횡방향 오차
    err_depth = GOAL_SCALE - area         # 전후방향 오차
    if converged(err_uv, err_depth): break
    delta = IMAGE_JACOBIAN @ [err_uv, err_depth]
    arm.move_delta(delta * GAIN)          # 작게, 여러 번
```

#### 왜 이게 좋은가

| 문제 | 이 방식의 대응 |
|---|---|
| 파지 편차 | Teach를 재파지마다 다시 하면 자동 흡수 |
| 접촉 밀림 | 밀리면 픽셀 오차가 다시 생기고 루프가 재수렴 |
| 기구학 캘리브레이션 오차 | 픽셀 오차로 나타나므로 동일하게 흡수 |
| 카메라 내부/외부 파라미터 | **불필요** (자코비안을 실측으로 구함) |
| 백래시 | 흡수됨 |
| 과제 요구사항 | 티칭 + 좌표기반 + 비전 기반 제어를 한 번에 충족 |

#### 이미지 자코비안 실측 (약 2분)

캘리브레이션 이론 없이 숫자로 채운다.

```
파지한 상태에서 각 방향으로 소량 이동 → 픽셀 변위 기록
  +5mm  로봇 X → (du, dv) 측정
  +5mm  로봇 Y → (du, dv) 측정
  +5mm  로봇 Z → (du, dv, darea) 측정
→ 3x3 행렬 구성 → 역행렬
```
부호와 게인만 맞으면 수렴한다. 정확할 필요 없다. **게인은 0.3 이하로 시작** (진동 방지).

### 4.3 각 카메라의 역할 분담

| 카메라 | 역할 | 단계 |
|---|---|---|
| **탑다운 (뎁스)** | 인두기 / 드라이버의 **대략적 위치** 파악, 파지 지점 결정, 테이블 평면 높이 | Coarse |
| **손목 (eye-in-hand)** | 최종 정밀 접근의 **폐루프 피드백** | Fine |

탑다운은 사양이 낮으므로 **정밀 제어에 쓰지 않는다.** 뎁스는 "물체가 테이블 위 어느 높이에 있나" 정도의 코스 정보로만 사용.

---

## 5. 비전 인식 방식

### 결론: 고전 CV(색 마커) 사용. YOLO 등 딥러닝 모델은 사용하지 않는다.

**근거**
- 정밀도: HSV 블롭 중심은 서브픽셀 정확도 / YOLO 바운딩박스 중심은 수 픽셀 지터가 있고, 자세가 바뀌면 박스가 늘어나며 중심이 따라 움직여 **물리적으로 의미 있는 점이 아니다**
- 속도: HSV는 CPU에서 카메라 프레임레이트로 동작 → 서보 루프 지연이 작다
- 데이터: 라벨링 불필요
- 씬을 완전히 통제할 수 있으므로 마커를 붙이면 그만

### 마커 부착 계획 (테이프/마커펜으로 즉시 가능)

| 대상 | 마킹 | 목적 |
|---|---|---|
| **드라이버 팁** | 진한 단색 마킹 1점 | 접근 타겟 |
| **인두기 샤프트** | 다른 색 테이프 밴드 1~2개 | 팁 위치 추정 / 슬립 감지 |
| **그리퍼** | (선택) 색 테이프 | 슬립 감지 기준점 |

**색 선택 주의**: 작업 환경에 **분홍색 마우스, 노란 마스킹테이프, 주황 클램프, 청/적 테이프 롤**이 있다.
→ 이 색들과 겹치지 않게 고르고, 검출 전 **ROI로 화면 일부만 잘라서** 오검출을 줄일 것.
→ 드라이버는 금속이라 **정반사(하이라이트)로 HSV가 깨진다.** 무광 테이프나 유성마커를 쓰고, 조명 반사를 피하도록 각도를 조정.

### HSV 튜닝은 반드시 도구로

트랙바로 H/S/V 상하한을 실시간 조절하며 마스크를 확인하는 **캘리브레이션 스크립트를 먼저 만들 것.** 값을 코드에 하드코딩하고 추측으로 고치는 것은 시간 낭비다. 튜닝 결과는 `config.yaml` 등에 저장해서 재사용.

---

## 6. 접촉 감지 & 밀림 대응

### 접촉 감지
Feetech STS3215는 `Present_Load` / `Present_Current` 레지스터를 읽을 수 있다.
- 하강 중 부하가 임계값 이상 급증 → 접촉 판정 → 즉시 정지
- **먼저 해야 할 일**: 공회전 시 / 접촉 시 부하값을 로깅해서 임계값을 실측할 것

### 컴플라이언스
하강 구간에서만 **토크 리밋 / P 게인을 낮춘다.**
- 접촉 충격이 인두기를 그립 밖으로 밀어내는 것을 완화
- 동시에 과제 요구사항의 안전 데모 요소로 활용 가능

### 슬립 감지
손목 카메라에서 **인두기 밴드 마커의 픽셀 위치는 원래 불변**이다.
→ 이 값이 임계 이상 변하면 = 슬립 발생
→ 대응: 동작 중단 → 사용자에게 알림 → Teach 재수행 또는 재파지

---

## 7. 구현 단계 (Fallback Ladder)

**아래를 순서대로 구현한다. 각 단계에서 반드시 동작을 확인하고 커밋한 뒤 다음으로 넘어간다.**
시간이 부족하면 그때까지 완성된 단계가 그대로 데모가 된다.

### L0 — Teach & Replay (비전 없음) 🔴 최우선, 데모 백업
- 파지 → 접촉까지 전 과정을 텔레옵으로 시연하고 관절 궤적을 녹화
- 그대로 재생
- **이것이 확보되면 최소한 발표할 것은 생긴다.** 무조건 먼저 끝낼 것.

### L1 — 파지 동작 스크립트화
- 인두기를 테이블 위 **테이프로 표시한 고정 위치**에 놓는다 (지그 대용, 무료)
- 그 위치로 이동 → 그리퍼 닫기 → 들어올리기를 좌표 기반으로 구현
- 파지 성공 판정: 그리퍼 서보의 최종 위치값이 예상 범위인지 확인 (놓치면 완전히 닫힘)

### L2 — 손목 카메라 비주얼 서보잉 🟡 핵심 기능
- 타겟 마커 HSV 검출
- 이미지 자코비안 실측
- Teach로 `GOAL_PIXEL` / `GOAL_SCALE` 획득
- 수렴 루프 구현
- **여기까지 되면 "파지 편차 보정" 요구를 만족한다.**

### L3 — 접촉 감지 + 컴플라이언스
- 부하 기반 정지
- 하강 시 토크 리밋 완화

### L4 — 탑다운 뎁스 카메라로 타겟 위치 자동 검출 (여유 시)
- 드라이버를 임의 위치에 놔도 코스 접근이 가능하게
- **데모 임팩트가 크다.** 시간 남으면 여기에 투자.

### L5 — 슬립 감지 (여유 시)

---

## 8. 과제 요구사항 체크리스트

프로젝트 채점 요구사항이다. 구현에 반드시 반영할 것.

- [ ] **티칭 / 좌표 기반 제어 활용** → L0, L1에서 충족
- [ ] **비전 기반 제어를 보여주는 시나리오** → L2가 핵심 증거
- [ ] **긴급정지 버튼** → 키보드 인터럽트 또는 GUI 버튼. 모든 루프에서 매 iteration마다 플래그 확인. 즉시 토크 오프 또는 정지
- [ ] **카메라 전송 / 로봇 제어 딜레이 발생 시 보호장치 동작 데모**
  → 프레임 타임스탬프를 확인해서 **N ms 이상 지연되거나 프레임이 끊기면 동작 정지**. 의도적으로 카메라를 가리거나 케이블을 뽑아 데모 가능
- [ ] **로봇 제어 limit 데모** → 관절 각도 한계, 최대 속도, 최대 이동량(delta clamp), 부하 한계를 코드에 명시하고 초과 시 거부되는 것을 시연
- [ ] **내부 공유용 개발 문서 작성** (필수)
- [ ] 발표 (PPT 없이 팀 프로젝트 소개)

> 안전 관련 코드는 나중에 붙이지 말고 **L0 단계에서 골격을 미리 넣어둘 것.** 나중에 넣으면 못 넣는다.

---

## 0. 코드베이스 파악 (⚠️ 가장 먼저 수행)

제공받은 기존 코드가 있다. **아무것도 새로 만들기 전에** 다음을 조사하고 사용자에게 보고하라.

### 조사 항목

1. **디렉토리 구조와 진입점**
   - 실행 가능한 예제 스크립트가 무엇인지, README가 있는지

2. **로봇 제어 레이어**
   - 어떤 라이브러리를 쓰는가? (LeRobot / feetech-servo-sdk / 자체 래퍼 / ROS2 노드)
   - **관절 각도 직접 제어 API**가 있는가? 함수 시그니처는?
   - **IK(역기구학)가 이미 구현되어 있는가?** 없다면 관절 공간에서만 움직이는 설계로 가야 한다
   - 카테시안 delta 이동 함수가 있는가? (`move_delta(dx, dy, dz)` 형태)
   - 그리퍼 개폐 API
   - 서보 레지스터 접근 가능 여부 → `Present_Load`, `Present_Current`, 토크 리밋 쓰기 가능한지

3. **카메라 레이어**
   - 손목 카메라 / 탑다운 카메라를 각각 어떻게 여는가 (device index, 해상도, FPS)
   - 탑다운 뎁스 카메라의 **정확한 모델명**과 SDK (RealSense? Astra? OAK?) — 뎁스 프레임 접근 방법
   - 두 카메라를 동시에 열 수 있는지 (USB 대역폭 문제 가능)

4. **기존 캘리브레이션 / 티칭 자산**
   - 로봇 캘리브레이션 파일이 이미 있는가
   - 텔레옵(리더암) 스크립트가 있는가 → **L0 녹화에 그대로 쓸 수 있으므로 중요**
   - 궤적 녹화/재생 기능이 이미 있는가

5. **좌표계 정의**
   - 베이스 프레임의 원점과 축 방향
   - 관절 각도의 단위와 부호 규약 (라디안? 도? 서보 raw ticks?)

### 보고 형식
조사 후 다음을 정리해서 사용자에게 알릴 것:
- 사용 가능한 API 목록 (시그니처 포함)
- **이 문서의 계획 중 기존 코드로 바로 되는 것 / 새로 만들어야 하는 것**
- 계획 수정이 필요한 지점

---

## 9. 알려지지 않은 것 / 확인 필요

구현 초반에 반드시 실측/확인할 항목이다. 추측하지 말 것.

- [ ] 손목 카메라의 FOV에 **인두기 팁이 실제로 들어오는가?**
  - 들어온다 → 팁과 타겟을 한 화면에서 보며 서보잉 (최상)
  - 안 들어온다 → 위 4.2의 `GOAL_PIXEL` 방식 사용 (팁이 안 보여도 동작함)
  - **가장 먼저 확인할 것.** 손목 카메라 영상을 그냥 띄워보면 된다.
- [ ] 손목 카메라 장착 각도와 방향 (영상이 뒤집혀 있는지 포함)
- [ ] 인두기를 든 상태에서 타겟까지 도달 가능한 자세가 IK로 존재하는가 (5-DOF 한계)
- [ ] 인두기 무게가 SO-101 페이로드 안에 들어오는가 — 처짐(sag) 발생 시 정적 오프셋 보정 필요할 수 있음
- [ ] 파지 시 그리퍼가 인두기를 실제로 유지하는가 (미끄러지면 파지 지점을 손잡이 그루브 근처로 변경)
- [ ] 서보 부하값의 정상/접촉 시 범위
- [ ] 두 카메라 동시 사용 시 USB 대역폭 / 프레임레이트

---

## 10. 코딩 원칙 (이 프로젝트 한정)

1. **동작하는 것을 먼저.** 완벽한 추상화보다 오늘 17시에 돌아가는 코드가 우선이다.
2. **모든 단계를 개별 스크립트로 실행 가능하게.** 한 번에 다 돌리는 통짜 스크립트는 디버깅이 불가능하다.
3. **시각화 창을 항상 띄운다.** 검출 마스크, 검출점, 오차 벡터를 화면에 그린다. 눈으로 봐야 고칠 수 있다.
4. **하드코딩된 상수는 전부 파일 상단 또는 config로.** 현장에서 계속 바꾸게 된다.
5. **로봇을 움직이는 모든 함수는 delta clamp를 통과시킨다.** 한 번에 움직일 수 있는 최대량을 물리적으로 제한.
6. **모든 while 루프에 최대 iteration과 타임아웃.** 무한 루프로 팔이 계속 밀고 들어가는 상황을 원천 차단.
7. 진행 상황과 측정값을 표준 출력으로 계속 찍는다.

---

## 부록. 참고 — 채택하지 않은 선택지와 이유

| 방안 | 불채택 사유 |
|---|---|
| 커스텀 그리퍼 조 / 3D 프린팅 지그 | 제작 불가 (제약 조건 1, 2) |
| ACT / Diffusion Policy 등 학습 정책 | 텔레옵 데모 50~100회 수집 필요, 정밀도 5~10mm로 부족, 디버깅 불가, 안전 한계 보장 어려움 |
| YOLO 기반 검출 | 바운딩박스 중심의 정밀도 부족, 라벨링 시간 없음. 씬 통제가 가능하므로 마커가 우월 |
| 월드 좌표계 TCP 캘리브레이션 (4점법 등) | 손목 카메라 IBVS가 이를 불필요하게 만듦. 시간 절약 |
| 사이드 카메라 추가 | 손목 카메라가 있으므로 불필요 |
